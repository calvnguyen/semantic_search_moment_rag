# secrets
.env

# python
.venv/
__pycache__/
*.pyc

# cached data / model artifacts
# (transcript.json is committed so the notebook works on Colab — YouTube blocks Colab IPs)
data/*
!data/transcript.json
.ipynb_checkpoints/
